import {BrowserRouter,Routes,Route} from "react-router-dom"

export default function router() {
  return (
    <BrowserRouter>
    <Routes>
        <Route path="/"/>
    </Routes>
    </BrowserRouter>
  )
}
